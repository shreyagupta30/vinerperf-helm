apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-two.fullname" . }}-test-connection"
  labels:
    {{- include "pod-two.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-two.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
