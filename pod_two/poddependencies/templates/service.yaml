apiVersion: v1
kind: Service
metadata:
  name: {{ include "poddependencies.fullname" . }}
  labels:
    {{- include "poddependencies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poddependencies.selectorLabels" . | nindent 4 }}
