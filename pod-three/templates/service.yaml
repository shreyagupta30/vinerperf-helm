apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-three.fullname" . }}
  labels:
    {{- include "pod-three.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod-three.selectorLabels" . | nindent 4 }}
