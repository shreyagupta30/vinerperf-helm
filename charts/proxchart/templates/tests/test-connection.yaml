apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxchart.fullname" . }}-test-connection"
  labels:
    {{- include "proxchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
