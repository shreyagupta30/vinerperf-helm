apiVersion: v1
kind: Service
metadata:
  name: {{ include "testpmdchart.fullname" . }}
  labels:
    {{- include "testpmdchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testpmdchart.selectorLabels" . | nindent 4 }}
